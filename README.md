# earl-greylyn

An [earl-grey](https://earl-grey.halt.wtf) colorscheme variant for Neovim.

## Usage

It's primitive and manual for now:

- Copy `earl-greylyn.lua` into `~/.config/nvim/colors`
- Add this to your `init.lua` or other config file: `vim.cmd("colorscheme earl-greylyn")`
